# Fly.io config for Betting Analyzer Bot
app = "reybetting-bot"
primary_region = "nrt"  # Tokyo — closest to Japan

[build]

# No HTTP service — this is a pure worker bot
[processes]
  worker = "python3 bot.py"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
